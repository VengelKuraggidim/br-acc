========================================
 DEPLOY — Fiscal Cidadão
========================================

Site: https://storage.googleapis.com/fiscal-cidadao-pwa/index.html
API:  https://fiscal-cidadao-api-xfzjqhaisa-rj.a.run.app


----- 99% dos casos -----

  bash scripts/deploy/deploy_all.sh

Detecta o que mudou e sobe. Termina na hora se nada mudou.

Depois: Ctrl+Shift+R no browser pra ver mudança no site.


----- Re-ingeriu ETL (grafo local mudou) -----

  bash scripts/deploy/deploy_all.sh --graph

Só passa --graph quando os pipelines RODARAM. Mudança em etl/src/
no código não basta. Essa flag wipa e re-copia o Aura (~3min) e os
links de políticos salvos quebram — busca continua funcionando.


----- Força bruta (refazer tudo) -----

  bash scripts/deploy/deploy_all.sh --all


----- Pré-requisitos (uma vez só) -----

  gcloud auth login
  gcloud config set project fiscal-cidadao-493716
  docker compose up -d neo4j        # só pra --graph


----- Quando algo dá errado -----

"senha do container" ou "não achou fiscal-neo4j"
  docker compose up -d neo4j && espera 30s

"cannot read secret" do gcloud
  gcloud auth list   # tá logado como fernandoeq@live.com?

"mudei mas browser não atualiza"
  Ctrl+Shift+R (service worker cacheia)
