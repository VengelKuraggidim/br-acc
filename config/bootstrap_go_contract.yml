{
  "contract_version": "2026-04-17",
  "contract_mode": "subset",
  "subset_scope": "goias_only",
  "full_historical_default": true,
  "expected_implemented_count": 5,
  "core_sources": [
    "camara_goiania",
    "folha_go",
    "pncp_go",
    "querido_diario_go",
    "tcm_go"
  ],
  "sources": [
    {
      "pipeline_id": "camara_goiania",
      "acquisition_mode": "script_download",
      "required_inputs": [
        "data/camara_goiania/*"
      ],
      "blocking_reason_if_any": "-",
      "core": true,
      "download_commands": [
        "cd /workspace/etl && uv run python ../scripts/download_camara_goiania.py --output-dir ../data/camara_goiania"
      ]
    },
    {
      "pipeline_id": "folha_go",
      "acquisition_mode": "script_download",
      "required_inputs": [
        "data/folha_go/*"
      ],
      "blocking_reason_if_any": "-",
      "core": true,
      "download_commands": [
        "cd /workspace/etl && uv run python ../scripts/download_folha_go.py --output-dir ../data/folha_go"
      ]
    },
    {
      "pipeline_id": "pncp_go",
      "acquisition_mode": "script_download",
      "required_inputs": [
        "data/pncp_go/*"
      ],
      "blocking_reason_if_any": "-",
      "core": true,
      "download_commands": [
        "cd /workspace/etl && uv run python ../scripts/download_pncp_go.py --output-dir ../data/pncp_go"
      ]
    },
    {
      "pipeline_id": "querido_diario_go",
      "acquisition_mode": "script_download",
      "required_inputs": [
        "data/querido_diario_go/*"
      ],
      "blocking_reason_if_any": "-",
      "core": true,
      "download_commands": [
        "cd /workspace/etl && uv run python ../scripts/download_querido_diario_go.py --output-dir ../data/querido_diario_go"
      ]
    },
    {
      "pipeline_id": "tcm_go",
      "acquisition_mode": "script_download",
      "required_inputs": [
        "data/tcm_go/*"
      ],
      "blocking_reason_if_any": "-",
      "core": true,
      "download_commands": [
        "cd /workspace/etl && uv run python ../scripts/download_tcm_go.py --output-dir ../data/tcm_go"
      ]
    }
  ],
  "excluded_from_contract": [
    {
      "pipeline_id": "tce_go",
      "reason": "not_built in registry; no pipeline module exists in etl/src"
    },
    {
      "pipeline_id": "state_portal_go",
      "reason": "not_built in registry; no pipeline module exists in etl/src"
    }
  ]
}
